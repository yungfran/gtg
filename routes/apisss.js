const express = require("express");
const router = express.Router();

//First time login route for inputting data

//Completed 1 GTG set, increment total reps for that excersize by current reps per set

//Incomplete GTG set, increment total reps by number of reps completed
